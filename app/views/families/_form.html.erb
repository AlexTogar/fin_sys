<%= form_for(@family, html: { class: "form-horizontal inside-form", role: "form" }) do |f| %>
  <% if @family.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@family.errors.count, "error") %> prohibited this family from being saved:</h4>

      <ul>
      <% @family.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, class: "control-label" %>
    <div class="">
      <%= f.number_field :name, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :connect, class: "control-label" %>
    <div class="">
      <%= f.number_field :connect, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :user, class: "control-label" %>
    <div class="">
      <%= f.text_field :user, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :deleted, class: "control-label" %>
    <div class="">
      <%= f.check_box :deleted, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="">
      <%= f.submit class: "btn btn-primary gradient" %>
    </div>
  </div>
<% end %>
