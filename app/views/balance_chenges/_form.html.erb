<%= form_for(@balance_chenge, html: { class: "form-horizontal inside-form", role: "form" }) do |f| %>
  <% if @balance_chenge.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@balance_chenge.errors.count, "error") %> prohibited this balance_chenge from being saved:</h4>

      <ul>
      <% @balance_chenge.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :sum, class: "control-label" %>
    <div class="">
      <%= f.number_field :sum, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="">
      <%= f.submit class: "btn btn-primary gradient" %>
    </div>
  </div>
<% end %>
